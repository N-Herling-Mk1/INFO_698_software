# `site_export/`

Curated, committed JSON for the static (no-server) public view on GitHub Pages.
`export_site.py` copies a curated slice of run.json / compute.json / eda_stats.json /
figures here, then they're committed to the documentation repo. This is what makes
genealogy + EDA + scorecards visible with no backend running.
