[project]
name = "forge-software"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "flask",
  "gunicorn",
  "pandas",        # read artifacts; NO torch, NO librosa — keep this image light
  "pyyaml",
]
